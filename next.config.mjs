/** @type {import('next').NextConfig} */
const nextConfig = {
  // Produces a self-contained server bundle in `.next/standalone` for
  // self-hosting (Hostinger VPS, any Node.js host). Run with:
  //   node .next/standalone/server.js
  output: "standalone",
};

export default nextConfig;
